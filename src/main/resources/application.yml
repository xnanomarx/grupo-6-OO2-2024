spring:
  datasource:
    url: jdbc:mysql://localhost/baseprueba?serverTimezone=GMT-3
    username: root
    password: 150721
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.MySQL5Dialect
  thymeleaf:
    cache: false