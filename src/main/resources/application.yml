spring:
  datasource:
    url: jdbc:mysql://localhost/grupo6?serverTimezone=GMT-3
    username: root
    password: root12345678
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.MySQL5Dialect
  thymeleaf:
    cache: false